# FuturesMonitor service dependencies
# Note: Most dependencies are already in the main pyproject.toml

# Core async
asyncio-redis>=0.16.0
aiohttp>=3.8.0

# Database
asyncpg>=0.27.0

# Redis
redis>=4.5.0
msgpack>=1.0.0

# HTTP
tenacity>=8.0.0
